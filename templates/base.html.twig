<!DOCTYPE html>
<html>
    <head>
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <meta name="description" content="Sites de   de Baby Shower">
        <meta name="keywords" content="Baby Shower prestation de service">
        <meta name="author" content="emmanuel de sousa">

        
        <title>PRETTY BABY{% block title %}{% endblock %}</title>
        <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text></svg>">
        <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css">
        <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css">
        <link rel="stylesheet" href="{{ asset('css/style.css') }}">
         
        {% block stylesheets %}{% endblock %}
    
    </head>
    <header >
        <nav class="row" id="nav">
            <div class=" col-2 col-md-2 col-xl-2" id="logomenu">
                <a href="{{ path('home') }}" >
                    <img class="logo p-5" src="{{ asset('logo/logo.png') }}" alt="logo du site">
                </a>
            </div>
            <div class="col-6 col-md-4 col-lg-10 col-xl-10 p-5" id="menu">
                <a class="btn" href="{{ path('home') }}#realisations">NOS REALISATIONS</a>
                <a class="btn" href="{{ path('home') }}#offres">NOS OFFRES</a>
                <a class="btn" href="{{ path('contact') }}">NOUS CONTACTER</a>
                
                {% if is_granted('ROLE_USER') %}
                    <a class="btn" href="{{ path('user') }}">PROFIL</a>
                {% else %}
                    <a class="btn" href="{{ path('app_login') }}">CONNEXION</a> 
                {% endif %}
            </div>
        </nav>
    </header>

    <main>
        {% include 'shared/flash.html.twig' %}
        {% block body %}{% endblock %}
    </main>

    <footer class="container-fluid" id="footer">
        <div class="row">
            <div class="row text-center" id="menufooter">
                <div class="col-12 col-md-5 col-lg-5 col-xl-5" id= "apropos">
                    <h2>A propos</h2>
                    <a href="ml.html">mentions legales</a></br>
                    <a href="dp.html">données personnelles</a></br>
                    <a href="cookies.html">cookies</a></br>
                    {% if is_granted ('ROLE_ADMIN') %}
                    <a href="{{ path('admin') }}">admin</a>
                    {% endif %}
                </div>
                <div class="col-12 col-md-2 col-lg-2 col-xl-2">
                    <a href="mailto:info@prettybaby.com">info@prettybaby.com</a>
                </div>
                <div class="col-12 col-md-5 col-lg-5 col-xl-5">
                    <h2>
                        suivez nous:
                        <a href="https://www.facebook.com"><i class="fab fa-facebook"></i></a>
                        <a href="https://www.instagram.com/"><i class="fab fa-instagram"></i></a>
                    </h2>
                </div>
            </div>
            <div class="row">
                <div class="col-12 text-center">
                    <p>Copyright © 2022 - Emmanuel De sousa</p>
                </div>
            </div> 
        </div>
    </footer>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
    <script src="{{ ('js/script.js')}}"></script>

        {% block javascripts %}{% endblock %}
</html>
